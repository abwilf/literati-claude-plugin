{
  "name": "literati-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Literati MCP server (stdio) for the Claude Code plugin",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  },
  "scripts": {
    "build": "esbuild index.mjs --bundle --platform=node --format=esm --target=node18 --outfile=bundle.mjs --banner:js=\"import { createRequire } from 'node:module'; const require = createRequire(import.meta.url);\""
  }
}
