{
  "name": "literati",
  "version": "0.6.3",
  "description": "Use Literati's agent tools (edit, compile, papers) from Claude Code, with transcript sync back to Literati so sessions can be resumed there. The MCP server registers at user scope as 'literati' via /literati:login; this plugin provides the hooks, login command, and bundle updates.",
  "author": { "name": "Literati" }
}
